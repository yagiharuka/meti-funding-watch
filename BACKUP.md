# 行政事業レビュースナップショットのバックアップ

`data/review-cache/` は、RSシステムの配布経路変更後に再取得できない2026-08-06取得分を含みます。Git履歴だけを唯一の保管先にしません。

## バックアップ作成

```bash
bash scripts/create-review-backup.sh /安全な出力先
```

このコマンドは、ファイル順、所有者、mtime、gzipヘッダーを固定したtarballとSHA-256ファイルを作ります。同じ入力から同じバイト列を再生成できます。

## 保管要件

- GitHubとは別のサービス・別の認証境界に1部以上保管する
- tarballと`.sha256`を同時に保管する
- 復元テストで`sha256sum -c`とJSON件数検証を行う
- 更新後のスナップショットを追加保存し、過去分を上書きしない

GitHub Actions artifactや同じリポジトリのReleaseだけでは、アカウント停止・リポジトリ削除へのオフサイト対策になりません。
